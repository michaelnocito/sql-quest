{
  "name": "sql-space-quest",
  "version": "2.0.0",
  "description": "SQL Space Quest — a browser game where real SQL queries clear the swarm. Learn analyst SQL by playing.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "setup": "playwright install chromium",
    "serve": "node test/server.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.49.1"
  }
}
